"""Document links for reST references in esbonio, with line-precise targets.

- ``esbonio_ref_links.document_links`` — esbonio server feature, loaded with
  ``esbonio server --include esbonio_ref_links.document_links``.
- ``esbonio_ref_links.object_locations`` — Sphinx extension, added to the
  ``extensions`` list in ``conf.py``.
"""
